#include<stdio.h>
#include<string.h>
int main()
{
    // Given Four caracter and three of those caracters, find missing caracter

    //  char a[10], b,c,d;
    //  scanf("%s %c %c %c", &a, &b, &c, &d );
    //  for(int i=0; i<strlen(a); i++)
    //  {
    //     if(a[i] !=b && a[i] !=c && a[i] !=d)
    //     {
    //         printf("%c", a[i]);
    //         break;
    //     }
    //  }
// end check caracter



    // Given Four number and three of those number, find missing number

    // int a ,b,c,d;
    // scanf("%d %d %d %d", &a,&b,&c,&d);
    // printf("%d", a/(b*c*d));

// end check number


return 0;
}